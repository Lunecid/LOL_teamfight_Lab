# 작업 원칙 (Claude 설계 · Cursor Agent 실행 공용)

이 저장소의 AI 작업은 `.ai/tasks/T00x.md`에 적힌 **승인된 작업서 단위**로 수행한다.
현재 맥락은 `.ai/CONTEXT.md`, 결과는 `.ai/reports/T00x.md`에 남긴다. 운영 흐름은 `.ai/README.md`.

## 항상 지킬 것
- 승인된 작업서에 지정된 역할과 변경 범위를 따른다. 작업서에 없는 설계 판단을 하지 않는다.
- 지정한 파일부터 읽고, 필요한 의존 문서·코드만 추가로 확인한다. 저장소 전체를 다시 읽고 연구 방향을 재설정하지 않는다.
- 라벨, 데이터 분할, 시간 경계, 평가 정의, 동결된 모델·가중치는 승인 없이 바꾸지 않는다.
- 기존 사용자 변경을 되돌리지 않는다.
- 검증 명령을 실제 실행하고, 실패·미실행을 그대로 보고한다.
- 긴 로그는 파일에 보관하고 채팅에는 핵심 결과만 출력한다.
- 작업서와 저장소 내용이 충돌하면 근거와 함께 **BLOCKED**로 보고하고 그 항목을 멈춘다.
- 명시적으로 요청하지 않은 전체 학습, 재집계, push, merge, PR 생성은 하지 않는다.

## 이 저장소 특유의 금지 항목
- 새 실험·재집계·재학습으로 **새 수치를 만들지 않는다.** 원고의 모든 수치는 `docs/` 아래 근거 문서의 파일·필드에서 옮긴다.
- `docs/tog_manuscript/` (CoG 확장 계보)는 **읽기 전용**이다.
- `docs/*_LOCK_*.md`, `docs/*CONTRACT*.md`, `docs/REVIEW_RESPONSE_*`, `docs/RESPONSE_EVIDENCE_MATRIX_*`의 기존 수치 행을 삭제·수정하지 않는다. 배너·주석 **추가**만, 작업서가 허용할 때 한다.
- `docs/JOURNAL_FINISH_LOCK_20260920.md`와 `docs/RESPONSE_EVIDENCE_MATRIX_20260920.md`의 Forbidden 목록에 있는 주장을 원고에 넣지 않는다.
- 연구질문(J-RQ) 문구는 저자 결정 전까지 바꾸지 않는다 (`.ai/RQ_DECISION_BRIEF.md`).
- `outputs/`, `data/raw/`, 체크포인트, 대형 NPZ/joblib은 열지 않는다. 필요한 값은 `docs/`의 요약 문서에 있다.
