# R<NN> <작업 제목>

> 이 파일은 "여기(계획·설계)" 와 "Cursor(구현·실행)" 사이의 인계 명세다.
> Cursor 에는 이 파일 전체를 붙여 넣는다. 결과는 아래 "결과" 절에 적어 다시 가져온다.

## 1. 목적과 배경
- 왜 하는가 (어떤 주장/표/그림을 위해서인가): `docs/CLAIMS.md` C? / 절 ?.?
- 현재 상태와 문제:

## 2. 변경 대상
| 파일 / 함수 | 현재 동작 | 바뀔 동작 |
|---|---|---|
| `path/to/file.py::func` | | |

## 3. 실행
```bash
# 명령 (환경 변수, 시드, 분할 모드 포함)
```
- 예상 소요 / 자원:
- 산출물 경로:

## 4. 완료 기준
- [ ] 테스트: `pytest tests/...` 통과
- [ ] 산출물 존재: `outputs/.../summary.json`
- [ ] 수치 범위 또는 형식 조건:

## 5. 논문 반영 계획
- 갱신할 매크로 (`latex/config/numbers.tex`):
- 갱신할 객체/절 파일:
- `docs/NUMBERS.md` 출처 기록:

## 6. 결과 (Cursor 에서 채움)
- 실행 일시 / 커밋:
- 핵심 수치:
- 산출물 경로:
- 특이 사항 / 실패:
