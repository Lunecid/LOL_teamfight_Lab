# Explicit objective-channel ablation — fixed execution specification

2026-09-15. User requests only experiment 2: no human review. Codex designs/audits; Claude implements AND RUNS to completion. This is exploratory after prior TEST exposure. No claim of fresh confirmation, semantic ground truth or causal objective effects.

## Scope

Read existing docs/LABEL_VALIDITY_FINDINGS_20260915.md, docs/TOG_END_TO_END_READINESS_AUDIT_20260915.md and relevant lv20260915 implementation. Reuse frozen extraction and proven read-only loading code. New output root outputs/objective_channel_ablation_20260915, new scripts prefix oc20260915_. All parent full/cohort/label-validity outputs, existing code, raw caches, original repo, manuscript and GPU queue are READ ONLY. No human packet/ratings. No installs/deletes/commits/pushes/external messaging. CPU <=4; no GPU; Python C:/Users/todtj/anaconda3/python.exe. Avoid writing parent outcome logs or import caches. Do not merely deliver scripts/PID: finish computations, verification and report, fixing routine errors and retaining failure logs.

Claude transfer consent persists: relevant spec/code/aggregate results and limited timeline excerpts may go to Claude; exclude credentials/account identifiers; never upload full raw corpus. Use local tools for computation.

## Fixed comparison: A vs B_noobj

A is untouched primary expanded StateV2 logistic C=.01, raw, final +5 OOF.
B_noobj uses the same logistic C=.01, weighting, train-only preprocessing, fold membership and hyperparameters, but removes EXACTLY all columns containing baron, elder, dragon, soul, herald, horde or atakhan (case-insensitive). This includes per-slot baron/elder death-since-acquisition and all objective/time interactions. Existing names yield 361 primary columns minus176 removed =185 retained. Freeze ordered lists, source trace, hashes and this rule BEFORE fitting. Fail if counts differ until documented source discrepancy is resolved, never choose features using test.

Keep every other A input verbatim: kills/deaths/alive/HP/MP, economy/growth/CS/champions, time/time-squared, towers/plates/inhibitors and their interactions. As in A, snapshot_age_s is not included. The mixed global unknown_objective_team_count is retained because it combines unidentified structure/objective/soul events and cannot isolate neutral objectives without altering another input. Report its semantics and nonzero prevalence in TRAIN and evaluation sets. This is an EXPLICIT NAMED OBJECTIVE-CHANNEL ABLATION, not deletion of every possible objective proxy. Gold/XP/CS, structures, time and the mixed missing-team diagnostic can retain indirect information. No assertion of conditional causal effect or fully objective-free data.

Source clarification (no design change): StateV2 filters unassigned soul events before calling the legacy state builder. Therefore the retained counter in actual V2 primarily records unidentified elite-monster and structure events; the legacy helper also has an unknown-soul branch, which is bypassed for those filtered events. See gameplay/state_value_v2.py and gameplay/state_value.py. Quantify actual prevalence instead of assuming it is zero.

PRIMARY comparison uses RAW B probabilities, matching A's raw calibration, so the feature block is the only changed design choice. Secondary only: fit positive-slope sigmoid using existing V_CAL and compare raw vs sigmoid on V_SELECT under previous logloss/Brier/name rule. Freeze chosen family before TEST; report separately and never substitute its primary raw results. Strictly increasing calibration cannot change delta sign when shared across endpoints except clipping/finite precision; verify this rather than presenting it as independent label evidence. No hyperparameter search or q training.

Use ALL exact primary TRAIN bucket queries (424160 queries/74168 matches), same final and5OOF fits, same equal-match weights and preprocessing. Every TRAIN engagement's endpoints use same adapter excluding its own match. All designated 210000 main matches and existing four external sets (21190 matches, including KR16.14 pilot) retained through existing extraction/validity masks. TRAIN15.14/VALIDATION15.15/TEST15.16; no random patch mixing, no new subsampling except TRAIN smoke checks.

## Execution gates

1. Save protocol, original/retained/dropped ordered columns, parent artifact inventory/hashes, references, analysis choices before full fit. Meaningful synthetic/TRAIN checks for exact feature removal, same retained values, OOF membership, preprocessing and key contracts. Inspect no new TEST comparisons until new models frozen.
2. Train final+5OOF B; convergence and reload checks. Existing iteration escalation only for nonconvergence, log each attempt. Freeze all adapters/calibrators/memberships and check hashes before sealed evaluation. Do not repoint globals in ways that write to parents; new root access logs only.
3. Evaluate raw A/B on identical saved main/external W bucket keys: AUC, logloss, Brier and calibration/time-band summaries. Report row and equal-match weighting explicitly. Main TEST paired match bootstrap1000 for B-minus-A Brier/logloss; fixed-model uncertainty, not training uncertainty. Secondary calibration separate.
4. Compute B raw p_pre,p_post,delta,Y for all7 sets and all existing h60/h90/h120 endpoints. Primary h90 unchanged. Preserve exact keys/endpoints/valid masks and primary A bit-for-bit; Y=1[delta>0], report exact zeros. Store all predictions for independent recalculation.
5. Report A/B sign disagreement, positive rates, delta difference and 1000 paired match bootstrap for main h90 E/T/N disagreement. Row and equal-match results, denominators and sparse groups. Same strata as previous study: abs(A delta), time, pre probability, ending reason, post>L frame freshness and SAME pre/post frame separately. Do not use test-based exclusion/threshold changes.
6. Use previous observed arrays with exact key verification (not new raw decoding) for acquisitions in (pre,e] and (L,e]: Baron, every elemental dragon/OTHER, elder, herald, horde/grubs, Atakhan, owned soul; unknown teams/soul0/both/multiple are diagnostic. For each, report counts, disagreement, signed delta A/B and small-delta proportions. Acquisition-team-oriented descriptive delta differences may be shown with Blue/Red sign transformation; explicitly not event causal value, timing counterfactual or pure immediate object reward. No-objective-acquisition rows may still have pre-existing objective history. Overlapping strata not additive. Sparse/pilot results no broad conclusions.
7. Score frozen main h90 specialist q against B labels for T and N on exact keys, reproducing A scores. No q refit/ranking; this is target dependence, not best q performance for B.
8. Verify unchanged parents, full memberships, masks/endpoints, adapter role/reloads, independently recomputable saved metrics and bootstrap. Save validation.json, commands/failures/status, feature evidence and Korean REPORT.md with before/after, exact tables, limits and paths. No human review performed or needed to finish this task.

## Evidence and interpretation

Reuse verified references and accurate citations from parent DEFINITION_AND_EVIDENCE.md/ToG evidence ledger: Maymin2021 for state-win-probability change valuation, Kim et al CoG2020 for win-probability quality, Jacobs/Wallach for measurement versus construct. These papers do not prescribe our176 columns,90s,C=.01 or subgroup thresholds: mark these as our design choices or inherited operational definitions. No new literature claims required.

Primary question: does explicitly encoding neutral-objective information improve held-out state-W probability quality, and how sensitive are model-defined engagement labels to that feature block? Both favorable and unfavorable results retained. W performance improvement is evidence of predictive utility, not proof that individual engagement labels are correct. Skipping human review is user's scope decision; leave semantic validation unperformed. Existing primary label/horizon/models remain frozen whatever results.
