# Initial probability and time baseline versus full-state engagement predictors

2026-09-15. User authorizes implementation AND full training now. Codex designs and audits; Claude implements/runs locally to completion. This is exploratory follow-up after existing TEST exposure. Do not claim untouched confirmation. This bounded first stage instantiates the immediately requested strengthened baseline and same-352-input logistic/LightGBM comparison. It does not complete the larger CoG/MLP comparison protocol.

## Scope and immutable parents

New output root outputs/incremental_q_training_20260915; new scripts prefix iq20260915_; tests may use tests/test_iq20260915_contracts.py. Use C:/Users/todtj/anaconda3/python.exe. CPU total <=4, no GPU, no installs/deletes/commits/pushes. Existing full_corpus_training_20260915, cohort_role_training_20260915, label_validity_full, objective_channel_ablation, collaborator_critique_audit, source implementations, raw data and original manuscript are read-only. Pure helper reuse is encouraged; never invoke helpers that append parent logs or repoint parent globals. Imports must not create parent __pycache__; use -B/PYTHONDONTWRITEBYTECODE. Preserve failures and support safe stage resume without changing selections after evaluation.

Existing Claude transfer approval covers this specification, relevant code, aggregates and limited timelines. Never transmit credentials/account identifiers or upload the raw corpus. Local tools can read numeric arrays for computation without emitting raw match identifiers in model responses. Complete fitting, selection, sealed evaluation, validation, and Korean report; scripts, preflight, PID or a plan alone are not completion.

## Fixed scientific contract

1. Main designated corpus remains 210000 matches: TRAIN15.14, VALIDATION15.15, TEST15.16. All eligible existing TRAIN rows are used in final refits. T and N fit separately; h90 only in this bounded stage. Existing h60/h120 definitions remain frozen but new model refits for those horizons are deferred and explicitly reported as not executed.
2. T is known min cluster count >=4; N known min<4; use parent cohort arrays with exact keys. TRAIN expected T39605/N159753; Q_CAL T10390/N40565; Q_SELECT T10195/N40180; TEST T32981/N130595. No balanced-only training, per-class or delta-magnitude reweighting, new exclusions or altered boundaries. Any count mismatch blocks fitting until traced.
3. Use parent Y=1[delta_h90>0], masks, p_pre and all feature values unchanged. Parent TRAIN p_pre/Y use own-match-excluded OOF V; verify adapter membership/hash contract rather than fitting V. Do not include post variables or actual final winner W in q inputs.
4. Full-input learners use exactly ordered predictor_sets['ridge'] from parent q_pre_only_schema.json, 352 numeric columns including p_pre_V, excluding direct champion ID and snapshot_age. Assert finite input; stop and document unexpected nonfinite values rather than silently altering the parent schema. Fixed median imputation inside fitted preprocessors is allowed as an explicit identical precaution; fit only on the applicable TRAIN subset. Scaling for logistic models only. No draft/role/position additions.
5. External evaluation separately uses EXT_KR_16.13, EXT_KR_16.14_pilot, EXT_KR_16.15, EXT_NA1_16.13. No new collection or EUW claim. These have prior exposure. Retain all same valid h90 T/N rows, with exact feature/label/cohort joins and duplicate checks.

## Three newly fitted families

Common C grid for logistic families: [0.0001,0.001,0.01,0.1,1,10]. Six configurations per family. Logistic uses lbfgs, tolerance1e-8, max_iter5000, random_state7. Log iterations, warnings and convergence. For nonconvergence only, allow a predeclared retry at max_iter20000, unchanged tolerance/data/settings; retain first attempt. A still-nonconverged candidate is a documented failure, not silently selected. Do not choose solver/tolerance from TEST performance.

A. Strengthened baseline PT: ONLY p_pre_V and pre time_minutes. For each marginal fit SplineTransformer(n_knots=5,degree=3,knots='quantile',include_bias=False,extrapolation='constant') on cohort TRAIN. Concatenate marginal bases Bp, Bt and all pair products Bp_i*Bt_j in documented p-major/t-minor order; expected6+6+36=48 columns if no schema degeneracy. Standardize this design on TRAIN then fit weighted logistic for each C. The model can learn initial probability, game phase and their interaction. Save knots/scaler/design names and dimensions. Duplicate knots or degeneracy is a documented implementation issue, not license to look at TEST or modify the six-candidate grid silently.

B. Full logistic: same 352-input information contract, TRAIN median imputer and scaler, six C candidates with the same solver/convergence policy as PT.

C. Full LightGBM: same 352 columns, same TRAIN median imputer, no scaler required. Six configs num_leaves[15,31,63] x min_child_samples[50,100], learning_rate.04, max1000trees, reg_lambda1, colsample_bytree.9, subsample1, objective binary. Seeds7,42,123; sequential fits with n_jobs<=4. Candidate prediction is mean raw probability across three seeds, never best seed. Store seed probabilities, runtime, best iterations, and seed-level descriptive metrics.

All families use equal total training weight per match, normalized to row mean1 as fc20260915_common.weights. This applies separately to every fit subset. Baseline versus full-information comparison deliberately differs in input information; full logistic versus full LightGBM holds 352 inputs fixed. Six configs is a bounded search convention, not proof of equal computation or global optimality.

## Training, calibration and selection

1. Freeze protocol.json, exact config/feature/schema hashes, split/cohort counts, expected parent hashes, seed/weighting/metric policies BEFORE any full fit. TRAIN-only contract tests and smoke must precede full execution; smoke cannot read actual VALIDATION/TEST. Reuse existing proven pure loaders or write run-local versions; cr20260915_data.load_eval_set writes to a parent gate and must not be called unchanged.
2. Tree stopping uses deterministic match hash sha256('iq20260915_stop:'+match_id), first8hex interpreted integer mod10==0 for internal stop10%; same match allocation for T/N. Fit preprocessing on fit90 only. Use explicit match-weighted Brier as the ONLY early-stopping metric, patience50, ties choose earlier iteration. Disable built-in competing stop metrics and log the evaluator. TRAIN-only internal stopping, never Q_CAL/Q_SELECT. For each config/seed refit from scratch on ALL cohort TRAIN rows, preprocessing refit there, n_estimators=chosen iteration. Store fit90/stop10/full counts and hashes. Neither a full-corpus subset nor smoke is a final model.
3. For every family/config, preserve raw Q_CAL/Q_SELECT predictions. Fit raw identity, sigmoid and isotonic calibration on cohort Q_CAL only. Use the existing q calibration/logit clipping convention consistently: reuse cr20260915_common.fit_calibrators or an equivalent documented copy. q sigmoid has no positive-slope constraint; store slope and clipping. Do not confuse with V calibration. For tree calibrate the raw three-seed mean, not individual seeds before averaging.
4. Each family has six base configurations x three calibration options =18 joint candidates. Freeze unique names containing both configuration and calibration and their lexical tie order before fitting. Rank all18 by full cohort Q_SELECT weighted Brier, then logloss, then that name. Recompute weights separately from Q_CAL match IDs for calibrator fits and from Q_SELECT match IDs for selection; never pass inherited TRAIN or whole-validation weights. Select an overall winner by the same rule, while preserving all family-winner comparisons. No model selection within balanced/time/external subsets. Freeze all selected bundles, configurations, calibration and hashes for BOTH cohorts before reading new TEST/external feature/label arrays for this run. Log accesses in new root. Prior knowledge of old TEST results is explicitly acknowledged.
5. Reload all saved selected bundles and verify identical predictions before sealed evaluation. Preserve parent fitted models/labels and verify their hashes unchanged.

## Evaluation

Main T is primary; N secondary. Report all eligible rows, B40=[.4,.6], B45=[.45,.55] based on frozen p_pre, plus pre-time bins[0,10),[10,20),[20,30),[30,infinity). Report B40 within each time bin too. Membership must be common across models, independent of new q and labels. No exclusion of small delta. Include empty and single-class cells.

Comparators: three new family winners; old frozen A specialist, pooled q, cohort-trained p_pre spline/logistic and constant, joined by exact match/s keys. Legacy baselines have an older fixed fitting budget and must be labelled as references rather than newly tuned families. Existing per-cohort predictions in A_<set>_h90_<T|N>.npz contain these. PT/full logistic/full tree are selected anew as above. Existing C role models are outside this bounded comparison.

Metrics in each cell: rows, distinct matches, positive count/rate, weighted Brier/logloss/AUC, calibration intercept/slope (descriptive fits only, never used to change evaluation predictions), fixed10-bin reliability/ECE, original0/1 probability counts. Equal total weight per represented match recomputed INSIDE each evaluation cell. Report no AUC for single-class cells. <30matches marked sparse and do not give inferential intervals or broad conclusions. Record degenerate bootstrap replicates.

Predeclared primary contrast: full LightGBM minus PT for T h90, on all and B40. Other planned contrasts: full logistic minus PT; full LightGBM minus full logistic; PT minus old p_pre spline; new LightGBM minus old A specialist. Report all comparisons regardless of direction; do not promote a better TEST pair to primary. N and B45/time/external are supporting/exploratory.

Use paired match bootstrap1000, seed20260915, same draw across all models in a cell. Save delta Brier/logloss/AUC and95% percentile intervals for primary contrasts on all/B40/B45 in main and external sets where >=30matches and classes permit. Time cells may have point/reliability tables only to bound cost; explicitly record CI not computed. Use efficient per-match loss sums and exact weighted AUC; do not reduce replicate count/rows silently. Fixed-model evaluation uncertainty, no multiple-comparison adjustment, not training/selection uncertainty. Preserve all predictions and enough metadata to independently recompute metrics.

SHAP, revised V, logit-label decomposition, raw timing correction, development-only detector retraining, MLP and full CoG lineup are deferred. Do not claim those complete. Current scope answers whether simple richer interactions and full-state learners improve over a stronger low-dimensional baseline. Final-W causal effect or semantic correctness of Y is not tested.

## Required gates and deliverables

Meaningful tests: exact352 and PT input whitelist48 design; train-only knots/scalers; tensor column order; fit/stop match disjointness; equal-match weights; tree stopping Brier and full refit; raw ensemble then calibration; Q_CAL/Q_SELECT separation; deterministic selection including ties; balanced boundary inclusion; row-key completeness; own-match OOF parent provenance; model reload; frozen-before-eval; parent preservation. Run existing relevant pure fixtures as appropriate but never rerun parent training.

Save protocol.json, source_hashes, commands/environment/failure logs, status.json with stage/PID/progress, candidate registry, internal-stop records, fit membership hashes, selection/frozen manifest, model bundles, raw/calibrated predictions, metrics and paired intervals, validation.json with real passed/failed checks, Korean REPORT.md and DEFINITION_AND_EVIDENCE.md with before/after. Do not declare success if a required check or model is missing. Logs and artifacts stay local; do not print raw IDs in external responses.

References and scope: inherited DELTA_Q_FAIR_BALANCED_PROTOCOL_20260915.md and COLLABORATOR_CRITIQUE_RESPONSE_20260915.md provide provenance. LightGBM method: Ke et al2017; logistic and spline implementation: scikit-learn official LogisticRegression/SplineTransformer documentation. PT tensor construction, knot count, C grid, cap, seeds and reporting cutoffs are our operational choices, not externally established optimal values. No additional literature or network research is needed for implementation. Keep an honest negative/mixed result if strong baseline wins.
