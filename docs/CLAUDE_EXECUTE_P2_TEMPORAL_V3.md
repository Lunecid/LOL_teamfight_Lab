# P2: temporal win-probability refactor and evaluation

Design owner: Codex. Implementation/execution: Claude Opus 5. Implement AND RUN. This is a new, exploratory version, not authorization to overwrite P1 or regenerate production engagement labels.

## Paths and scope

Use inherited TEAMFIGHT_WORKSPACE and launch Get-Location; never translate Unicode path names. Workspace is C:/Users/todtj/문서/LOL_Teamfight. Python C:/Users/todtj/anaconda3/python.exe. Library worktrees/engagement-state-value. Raw cache D:/LOL_Project/cache/match_cache_fresh_v3_engage_status13 is read only. Read docs/TEMPORAL_WINPROB_LITERATURE_ALIGNMENT_20260915.md, scripts/train_independent_v2.py, the V2 state/adapter modules and P1 protocol/results as needed. Check applicable AGENTS.md.

Write new scripts and versioned modules, tests and outputs/temporal_winprob_v3 only. Preserve P1/V1 code, models, predictions and existing state_value_v2_fix files byte-identical, including their source modules. Do not modify original Pycharm repo, user GPU queue, global configuration or install packages. No commits/pushes/deletion/secrets. CPU <=4 threads, BLAS nesting off. Checkpoint extraction and full logs. Fix routine implementation issues without weakening checks.

## Target and provenance

Target remains W=final Blue win; p_t estimates P(W=1 | observations available at t). Current snapshot V2 is f(X_t,t); new history comparator is f(X_{t-8min},...,X_t) at 1-minute intervals. These are approximations to history-conditional probability, not causal models. Final W may be read separately as training/evaluation target only. No final duration or percentage-of-final-match-time input, future padding, future normalization, future interpolation or postgame role slots.

Direct references and boundaries:
- Kim, Lee, Chung, CoG 2020, https://ieee-cog.org/2020/papers/paper_221.pdf : per-minute LoL probability and uncertainty-aware calibration. We evaluate probability quality but DO NOT claim to reproduce their uncertainty loss.
- Hodge et al., IEEE ToG 13(4):368–379, DOI10.1109/TG.2019.2948469: live professional Dota2 prediction. Cite live prediction motivation only; do not invent implementation detail.
- Maymin 2021 DOI10.1515/jqas-2019-0096: snapshot probability/value reference.
- Silva et al., SBGames2018, https://www.sbgames.org/sbgames2018/files/papers/ComputacaoShort/188226.pdf : SimpleRNN/LSTM/GRU history comparison, 9 time steps, 8 recurrent units, dropout .25. New experiment adapts this modest architecture, not exact reproduction across datasets.
- Jalovaara2024 master's thesis: existing 5-minute bucket sampling, supporting evidence not peer-reviewed journal evidence.

## Frozen protocol and candidates (write protocol before experiments)

1. Reuse P1 exact fit/calibrate/select/test matches and exact sampled query keys. Verify every history belongs only to its query's match and partition, and all engagement match sets remain disjoint from V fitting/calibration/selection/test. Preserve original sampled-row training weighting (unweighted final fit); history rows are not extra labelled samples. StateV2 schema and champion categorical encoding mandatory.
2. Candidate A is the exact frozen P1 expanded V2 adapter, no refitting or recalibration. Candidate B is one small SimpleRNN: 9 one-minute history positions ending at query t; 8 tanh recurrent units, input dropout .25, sigmoid output, binary cross entropy, RMSprop lr .001, batch64, fixed50 epochs, seeds17/29/43. These budget/seed/training choices are ours, not all from the paper. No test-directed architecture or hyperparameter search. For times preceding available nonterminal source support, pad with zeros plus explicit masking; fit preprocessing on FIT only; raw champion IDs must be categorical via fit-only encoding/embedding (describe method and unseen-ID behavior). No GPU. Use an already installed suitable runtime. If unavailable, report blocker for B but finish all independent A diagnostics without installing anything.
3. Fit raw/sigmoid/isotonic variants for B on CALIBRATE only, choose one calibration per seed using SELECT log loss. Primary B is the arithmetic ensemble of these three selected calibrated seed models (predeclared), report individual seeds too. Do not cherry-pick seed on TEST. Compare A vs B primary on SELECT log loss, exact tie keeps A. Freeze chosen candidate before reading new TEST evaluations. Report selection and test separately. This is exploratory because historical test has been examined.
4. Training and on-demand inference must share the same causal history assembly and preprocessing, with version/schema/model-hash guard. Champion roster/team membership must be known by query; preserve raw deterministic team/participant order. Keep snapshot age as audit, not predictor, for fair P1 comparison. Document this constraint and history missingness rather than concealing it.

## Evaluation and temporal interface

5. Score exact P1 one-query-per-test-match and full stored test grid. Report AUC/Brier/log loss, reliability bins, calibration intercept/slope, N. Add each 1-minute band and existing 2–10/10–20/20–30/30+ bands, equal total weight per match within cells. Low-count/one-class cells should return explicit NA, not fabricated metrics. Paired fixed-model match bootstrap >=1000 for primary overall differences; time/objective subgroup CIs exploratory, no multiple-testing certainty claims. Include original objective strata, especially elder and souls, without optimizing on them.
6. Export inspectable trajectory data and HTML/PNG plots for 12 test matches selected by deterministic hash ranking before metric inspection. Show p_A(t), p_B(t), source snapshot timestamps/ages, available event markers, no smoothed probabilities presented as observations. Time resolution of raw features and event updates must be stated; 1-second querying cannot manufacture 1-second observations.
7. Add an inference API that accepts chronological observed states/history up to each query, returns p_t with query/snapshot/model metadata, and computes delta between two endpoints with the SAME frozen adapter/calibration version. A model-version mismatch must fail. Out-of-order, cross-match, future-query history and wrong schema must fail. Do not force monotone p_t or smooth away real jumps. Plot/model update cadence separate from training sampling cadence.
8. Apply the already-frozen A and B to existing engagement rows in outputs/state_value_v2_fix (26,693 expected), ONLY for diagnostic endpoint transport, never V training/calibration/selection. Use exact stored pre/endpoint definitions, record timestamps and actual source freshness. Generate p_pre, p_post, delta and sign disagreement as DIAGNOSTIC outputs under this new output root; no replacement production labels/q training. Summarize endpoint Brier/log loss/calibration against final W with per-match weights (dependent two-endpoint rows); inspect model disagreement near delta zero, across time and snapshot-age strata. The final W evaluates p quality, not the unobservable true delta. Include objective events within endpoints when available, never claim causal attribution.
9. On a deterministic bounded sample, audit changes immediately before/at raw frame updates (integer ms query precision) and objective event timestamps. Distinguish observed co-occurrence from cause; report duplicate timestamps/concurrent updates, do not allocate probability jump to a single event when multiple inputs change. All auxiliary timestamps/labels excluded from predictors.

## Verification and output

- Record exact splits, history/query keys, data/module/model hashes, preprocessing fit origin, selection audit, resource budget and feature availability rules.
- Tests must prove future perturbation invariance, current-query equality between batch and incremental histories, no cross-match/history leakage, partition membership, masks, wrong-version rejection, save/load identity and frozen legacy hashes. Do not claim real-time API validation merely from retrospective causal replay.
- status.json updated at every stage and periodic long-run checkpoints; errors.json on failure. Save protocol.json before training; selection.json before TEST evaluation; training logs, predictions, trajectories, endpoint diagnostic results, metrics, validation.json, REPORT.md and a before_after.md linking sources and distinguishing implemented/validated/unresolved.
- If launched in background, monitor to completion and inspect final output; do not end with a promise of a watcher unless that watcher actually provides completion. Do not claim completion from a PID. A stage blocker must not erase completed independent diagnostics.
- Final report must distinguish reference-aligned implementation from scientific validation. Patches15.16/26.13 are not untouched tests. No all-patch claim; independent future-patch confirmation remains pending. Do not use subgroup degradation to tune on TEST. A or B may fail or lose; retain that outcome honestly.
