# P4: pre-engagement q baselines and explanation (exploratory)

Codex design; Claude Opus 5 implements AND RUNS. User authorized overnight work until 2026-09-15 08:10 KST. P3 full validation passed; Codex independently checked all 80,079 rows, label formulas, no extra kills, no future snapshots, exact B90 key/endpoint identity (outputs/engagement_labels_v3_sensitivity/codex_label_audit.json). Proceed with experimental labels only, not production or truth claims.

Workspace from TEAMFIGHT_WORKSPACE and Get-Location; Python C:/Users/todtj/anaconda3/python.exe. New files and outputs/q_v3_baselines only. Frozen original/P1/P2/P3/old validation files unchanged; do not modify existing user GPU queue, install packages, commit/push, delete files or inspect secrets. CPU <=4 and avoid BLAS nesting. Read only needed source docs/modules. Write protocol before fitting, source/model/data hashes, logs and status checkpoints; monitor to completion and inspect artifacts.

## Exact target, input and split

- q(X_pre)=P(delta_A>0 | pre-state, retrospectively selected engagement cohort). Primary is P3 A/B90, horizon90 remains fixed regardless of results. Exact zero ->0. Sample labels are model-defined outcomes, not observed true engagement success.
- Read P3 features_pre_only.npz and its schema, and labels_long.npz (or pandas round_trip CSV). No post-state, L, duration, endpoint/reason, future participant membership, final game outcome, event counts, target source diagnostics or match IDs as predictors. Keep IDs for grouping only. Positions excluded. Drop snapshot_age_s from predictors (audit only), explicitly report difference from old q experiment which included it. Champion IDs excluded in these numeric ridge/economic baselines, not treated as numerical ranks. p_pre_A is permitted and comes from V trained on disjoint matches.
- Use EXACT keys/patches/order from outputs/validation_suite_20260914/rows.csv and q_predictions.csv, joined to P3 pre rows, with no additions/drops. Recover original split: train15.14; 15.15 calibrate/selection split by sha256('calibration17:'+match) first8 hex mod2:0 calibrate,1 select; test15.16. Verify keys, patch memberships and counts against saved q_results: train9228 rows/3218 matches, calibrate4879/1669, select4922/1656, test7664/2655. Any material mismatch blocks fitting, not silently repaired. Ensure all q matches disjoint from ALL V partitions, and q partitions mutually disjoint.
- Train/evaluate equal total weight per match using the exact existing weights() convention, record normalization. Fit all preprocessing on TRAIN only. Restore existing methods from scripts/run_validation_suite.py without importing code that writes its old output directory.

## Predeclared candidates and selection

1. Constant TRAIN weighted positive prior.
2. p_pre_A logistic: StandardScaler + LogisticRegression C1 max_iter2000.
3. p_pre_A spline: SplineTransformer n_knots5 degree3 + StandardScaler + LogisticRegression C1 max_iter2000.
4. Numeric ridge: StandardScaler + LogisticRegression C.01 max_iter3000 on all allowed pre numeric state features plus p_pre_A, excluding champion IDs and snapshot_age. Preserve participant_slot naming and state schema.
5. Historical economic LightGBM subset: map old econ name selection in run_validation_suite.py to participant_slot prefixes, remove snapshot_age; spell out exact included names in protocol. It includes aggregate objective features and should be called economic/aggregate baseline, not gold-only. Ensemble seeds7/42/123, 250 trees,15 leaves,lr.04,min_child_samples100,reg_lambda1,colsample_bytree.9,n_jobs4. No early stopping/search/new architecture.
- For ridge and economic ensemble fit raw/sigmoid/isotonic variants; sigmoid C1e6 and isotonic fitted on CALIBRATE only with match weights. p_pre and spline are uncalibrated candidates as in prior work. Select lowest SELECT Brier then log loss, deterministic name tie break. Save selection before TEST prediction/evaluation. No old full/position model candidates (old schema/target). Record this reduced candidate pool explicitly; prior reported score is context, not an apples-to-apples gain.
- Main run A90. Repeat identical pipeline independently on A60/A120 as sensitivity, never select horizon on TEST. B90 label sensitivity: same allowed pre features including p_pre_A, train/select independently on B90 targets; describe that fixed inputs isolate target choice. Also score frozen A90 q against B90 labels as cross-target disagreement diagnostic, not comparable accuracy against truth. Do not repair/retrain frozen B despite its known rare plateau issue.

## Evaluation and SHAP

- AUC/Brier/log loss, calibration intercept/slope and reliability bins for each split; report train and select optimism. Test overall and existing time/patch/objective subgroups with N and appropriate NA for insufficient classes. Labels and probabilities must use one row per engagement, weighted by match.
- Paired match bootstrap >=1000 replicates on test for primary chosen vs constant, p_pre logistic/spline, selected economic/aggregate and selected ridge (skip identical pairs). Fixed models/selection, no refitting per bootstrap; no causal/all-patch/untouched-test claims.
- For A90 selected q, explain its actual prediction function on up to512 deterministic hash-selected TEST rows, background up to256 deterministic TRAIN rows only. If calibrated logistic: exact linear interventional SHAP on calibrated log-odds with train-derived baseline, preserve feature scaling, validate additive residual. If selected tree with nonlinear calibration: explain raw tree log-odds and clearly label it as base score, NOT exact final calibrated-probability explanation; optionally use a verified model-agnostic method with a declared budget. Spline/constant winner must be explained on its actual feature/output space. Do not choose a different model just to make SHAP attractive. Failures/blockers reported, not substituted with feature importance called SHAP.
- Save local cases and global mean|SHAP|, grouping mapping, feature units/names, output scale, baseline/background and additivity checks; group correlated variables cautiously, no causal or player-skill claims. Reference Lundberg & Lee2017 (existing verified reference registry) for SHAP; original calibration/value references retain stated boundaries. Never invent new citation claims.

## Required checks and report

Exact row order/labels to P3, exact split membership, no future predictors, no V/q match overlap, finite data/probabilities, transformer fitting origins, categorical IDs excluded, model serialization identity, selection-before-test, frozen hashes, independent metric reconstruction, SHAP additivity and deterministic examples. Labels target source/horizon/model hashes and q model/input schema must be stored in every manifest so V probabilities cannot be confused with q.

Output: protocol.json, status.json, selection per target before TEST, model bundles, pre-only schema, predictions (IDs separate), metrics/CIs, validation.json, SHAP artifacts, REPORT.md and DEFINITION_AND_EVIDENCE.md. Record implemented vs validated vs unresolved, all failed attempts and adjustments. P3 source staleness (~35% no frame after L), model-dependent labels, broad next-kill boundary and selected cohort persist; good q metrics do not solve these measurement issues. State readiness as exploratory only, list next independent future-patch/semantic validation rather than silently extending scope.
