# Full-corpus retraining — authorized 2026-09-15

User now explicitly requests full-data retraining. Codex specifies; Claude Opus 5 implements AND RUNS to completion. This authorization supersedes the expired overnight deadline and preflight-only stop. No match subsampling to the old pilot. Do not stop after writing scripts or dispatching Python. Preserve failed attempts and checkpoints, fix implementation errors, monitor actual execution.

## Scope, resources, artifacts

Read FULL_CORPUS_SPLIT_CORRECTION_20260915.md, EXTERNAL_TEST_PLAN_20260915.md, preflight REPORT/protocol and applicable AGENTS.md. Workspace from TEAMFIGHT_WORKSPACE; Python C:/Users/todtj/anaconda3/python.exe. CPU total <=4, no GPU, BLAS nesting off. New scripts/versioned modules and outputs/full_corpus_training_20260915 only. Preserve old models/source/artifacts and original Pycharm repo, raw D: inputs, user GPU queue. No installs, deletion, commits/pushes or secrets. Read raw inputs and write adapted external caches inside the new output root. Record source/data/model hashes and periodic status.json including stage, processed/total, PID, elapsed, error, next step. Resume checkpoints by exact manifest/protocol hash only.

Main manifest outputs/full_corpus_preflight_20260915/main_matches.csv is authoritative: TRAIN15.14=74673, VALIDATION15.15=74748, TEST15.16=60579, total210000. All matching eligible games must contribute to their designated roles. No-engagement games still belong to V dataset. All eligible detected engagements belong to q dataset in their designated roles. Semantic array/event validation remains to do; report every exclusion with match ID/reason, never quietly shrink corpus. Preflight exposures have566452 rows across194676 games; excluding348 overlapping next_start<=L is only an upper-bound count, not final label eligibility.

External manifests under outputs/full_corpus_preflight_20260915/external: KR API16.13=10064,16.14=200,16.15=926, NA1 16.13=10000; EUW0. Keep DB public_patch26.xx separate from API16.xx. Never use external or15.16 outcomes for fitting, preprocessing, calibration, choice of model/horizon/features. Previously accessed/unknown-use tests are exploratory, not untouched. Freeze all choices and selection manifests before scoring TEST; smoke tests may use TRAIN only, never full TEST.

## Frozen partition/dependency design (our operational choice)

TRAIN match fold = int(sha256('full-v-oof-20260915:'+match_id).hexdigest()[:8],16)%5. All states/engagements for a match share one fold.

VALIDATION match role = int(sha256('full-val-20260915:'+match_id).hexdigest()[:8],16)%4:
0 V_CAL, 1 V_SELECT, 2 Q_CAL, 3 Q_SELECT. These remain validation games, never TRAIN. Report counts for all roles. Every validation match is used for its assigned role; q performance on V_CAL/V_SELECT is diagnostic only and never q calibration/selection. This separation prevents final outcomes used in V calibration from also providing independent q validation labels. Do not copy pilot split/OOF assumptions.

V target W is final Blue win, kept separate from features. Primary V family is expanded snapshot StateV2 logistic, categorical champions, deterministic participant order, C=.01 (predeclared from exploratory pilot; not a new validated optimum), otherwise reuse existing adapter transformations and solver with convergence checks. No old model weights or fitted transformers reused. Fit final base V on all eligible15.14 matches; fit raw and positive-slope sigmoid calibration candidates using V_CAL only, choose lowest match-weighted log loss on V_SELECT, then Brier/name ties. Sigmoid on clipped raw logit, clip1e-12 solely for numeric transform; constrain slope positive using installed optimizer and record fit. Raw candidate includes identity. No isotonic V: potential flat regions erase delta; this is a documented design choice. No hyperparameter search on TEST. Freeze choice before labels/q fitting.

Fit five fresh OOF base V models: each uses only other four TRAIN folds, including preprocessing/vocabulary/scaling. If final V selected sigmoid, separately fit each fold model's sigmoid on V_CAL, with the same positive-slope rule. Apply selected calibration family uniformly. For each TRAIN engagement, its pre/post probabilities AND q input p_pre must come from its held-out-fold adapter. That match's W must never enter that adapter's fitting/calibration/preprocessing. For all validation/test/external engagements use final V trained on all TRAIN, calibrated as selected. Both endpoints must share the exact adapter/version. Save per-row fold/model hash, fitting-membership audit and calibration origin.

This is cross-fitting of generated targets, not proof of semantic truth. Fold-specific V versus final V can shift labels; report held-out TRAIN V metrics, endpoint distributions, fold dispersion and calibration. Do not require q TRAIN matches to be absent from every V model: require absence from the specific label-generating model. Final V will train on all TRAIN as intended. Do not switch q TRAIN to in-sample final-V labels for convenience.

V query sampling: use causal minute-frame queries >=2 minutes strictly before terminal event; fixed five-minute buckets [0,5),[5,10),... and choose one eligible query per nonempty bucket using deterministic hash(match_id,query_ms,'full-v-query-20260915'). Every game with eligible observations included. Fit/evaluate weights sum equally per match, including for V calibration/selection. Publish query keys, frame ages and sample counts. This reduces within-match duplication, not match coverage. Full minute trajectories on TEST are secondary evaluation with equal match weights; no duration/time-to-end as predictor. Sampling can use observed support retrospectively; explicitly disclose it is a training sampling scheme, not an inference input.

## State, detection, labels

Preserve objective_history_v2_participant_order schema, soul types/ownership fixes, observation-causal state extraction, no positions/future interpolation/postgame role assignments. Replay events known at query and last observed frames; audit staleness. Retain all supported objective classes including Baron, elemental dragons/soul, elder, herald, grubs, Atakhan. Inspect existing mapping: unsupported/missing classes cannot be silently zero-filled or claimed supported. Adapt external schema from raw data without using winner/target performance; record known-unavailable versus genuine zero. Structural inability is an explicit external-set blocker, continue main run.

Main cohort comes from full parent exposures, not old9198-match pilot. Preserve detector G13.7s,D4264,R1600,B15s,min2/team and actual source configuration; check against source rather than silently changing discrepancies. External detection uses same frozen definition. s=firstkill-15000ms, pre=s-1ms; L=lastkill. For h=60,90,120 endpoint=min(L+h*1000,next raw global CHAMPION_KILL strictly after L minus1ms,next eligible engagement start minus1ms,game_end minus1ms). Invalid/overlapping support excluded with full lineage. Objectives do not terminate windows. Primary h90 fixed; other horizons sensitivity, never optimize on test. delta=V(post)-V(pre), Y=1[delta>0], exact zero0. Save all boundaries, reasons, source timestamp/age, model hashes, endpoint equality and raw-kill exclusion checks. L/duration/end reason/post features/future roster/W are not q predictors. Observational change is not causal engagement effect.

## q fitting and evaluation

Adapt docs/CLAUDE_EXECUTE_P4_Q_BASELINES.md methods to FULL manifest and above dependencies, not pilot row lists/counts. All eligible15.14 rows train; Q_CAL calibrates, Q_SELECT selects. Numeric pre-only352-feature convention (verify actual names), categorical champion IDs excluded from numeric q, no position/snapshot_age. p_pre uses OOF V on TRAIN and final V elsewhere. Equal match weights.

Retain constant, p_pre logistic C1, p_pre spline5knots degree3 C1, numeric ridge C.01, economic/aggregate LightGBM ensemble seeds7/42/123,250trees,15leaves,lr.04,minchild100,lambda1,colsample.9. Raw/sigmoid/isotonic q candidates as pilot; q sigmoid C1e6 using existing code. Q_CAL only for calibration, Q_SELECT Brier then logloss/name for choice. Known pilot isotonic clipping failure must be disclosed; do not fix based on new TEST. Exact saved predictions; metric logloss uses standard documented numeric clipping, never alters saved probabilities. Independently train h60/h120 sensitivity after h90 main. Freeze all three target model selections before TEST evaluation; no target/feature tuning from test. Save comparison against old pilot as non-comparable scale/labeler change, not claimed improvement.

Evaluate V vs W and q vs generated Y separately. AUC/Brier/logloss, calibration slope/intercept/reliability, time/objective groups with N and missing classes. Main15.16 and each external region/patch separately. Match bootstrap1000 primary comparisons; fixed model/no retraining. Report raw matches, valid V matches/states, engagement matches/rows and exclusions for every role. All trained serialized objects must reproduce predictions. Cross-target sensitivity does not establish true engagement victory.

SHAP for chosen h90 q: reuse corrected P4 exact feature raw-logodds and final-probability group method as applicable, deterministic512 test cases and256 TRAIN background, additive identity checks; full background grouping/output scale disclosure. Do not relabel importance as SHAP. Explain model association, not causal esports/player skill. Save examples/plots and evidence register.

## Temporal comparator and ordering

Prioritize complete primary full-corpus V -> OOF labels -> q -> all test sets -> SHAP. Then retrain full-data temporal SimpleRNN comparator from P2 (9steps,8units,dropout.25,RMSprop.001,50epochs,seeds17/29/43) using same full TRAIN query keys and causal history, fit-only preprocessing, V_CAL and V_SELECT only, CPU<=4. Existing installed runtime only. Positive-slope sigmoid/raw per seed, choose V_SELECT logloss, fixed mean ensemble. Report independent V performance/trajectories against primary; never replace primary labeler after q/test results. Temporal architecture is a comparator and not a prerequisite for starting primary fitting. No additional B-label q runs required in this full run. Report comparator separately if computationally pending.

## Verification and record

Before full fit run meaningful contract tests on TRAIN-only fixtures: split membership, OOF self-outcome exclusion, future perturbation invariance, causal boundaries, no postgame predictors, transform fit origins, model serialization and independent metric reconstruction. Main extraction must actually decode files, not repeat header-only preflight. Write protocol.json before fitting and stage selections before TEST. Preserve lineage and objective availability audit, numerical convergence/errors and exclusions. Hash models before TEST and verify unchanged after.

REPORT.md and DEFINITION_AND_EVIDENCE.md must distinguish literature, our design choices, data estimates, completed experiments, unresolved limits. Reuse verified references in TEMPORAL_WINPROB_LITERATURE_ALIGNMENT_20260915.md and P3/P4 evidence documents with precise scope: Maymin2021 snapshot/value; Kim2020 probability/calibration motivation not reproduced uncertainty loss; Hodge2019/2021 live motivation; Silva2018 temporal comparator; Lundberg&Lee2017 SHAP; Jalovaara2024 thesis not peer-reviewed. Five folds, VAL hash partitions, positive-slope calibration and90s are our protocol choices, not established by these citations. No new literature claims without verified source. If contract impossible, report concrete evidence and complete independent stages; never silently substitute pilot runs. Completion means executed and validated artifacts, not code ready.
