# Separate engagement-scale training and role-aware q experiment

User authorization: separate engagement/teamfight training and strengthen teamfight prediction with lane/role importance; recover prior method. Codex designs; Claude Opus 5 implements AND RUNS. Continue through routine failures to executed results, not a plan or scripts-only handoff. Read the completed full-corpus REPORT/protocol and this specification. No expired overnight deadline applies.

## Fixed resources and lineage

Use TEAMFIGHT_WORKSPACE, Python C:/Users/todtj/anaconda3/python.exe, CPU total <=4, no GPU/installs/deletes/commits/pushes. Do not touch original repo, user GPU queue, old source or frozen results. New scripts/modules/tests and outputs/cohort_role_training_20260915 only. Preserve logs, status checkpoints, fit/selection timings, data/schema/source/model hashes and failed attempts. Do not inspect secrets. Prior full run outputs/full_corpus_training_20260915 is read-only and is the source of pre states, exact keys, labels, V models and q baseline predictions. Main corpus210000; same full patch roles15.14 TRAIN,15.15 VALIDATION,15.16 TEST, same four validation subroles and TRAIN folds. Same external KR16.13/16.14pilot/16.15/NA1 16.13 sets. No silent match sampling. Every exclusion counted.

Do NOT retrain V or change labels: retain frozen cross-fitted TRAIN V labels and final V elsewhere, h90 primary, h60/120 sensitivity. These labels are estimated probability improvement, not human ground truth. Tests were previously examined and this is an exploratory follow-up, not a fresh confirmatory evaluation. Future routing into a cohort is not a known live input.

## A. Restore exact scale definitions, then train separate q models

Authoritative scale: D:/LOL_Project/fusion_2615/corpus_shards_v33/manifest.json scale.teamfight_min=4, pick_max=1; original docs/tog_manuscript/sec_definition.tex scale subsection (around lines406-450) and scripts/scale_participation_v33.py, scale_cut_sensitivity_v33.py. Old ENGAGEMENT_SCALE_DEFINITION.md is explicitly stale v2 (cut3); DO NOT reuse cut3 as primary. Existing main detector stays G13.7s,D4264,R1600,B15s,M2. Participation count is not the pre-cutoff presence count. Use exact stored cluster_blue/cluster_red meaning, including auxiliary in-radius actors and current shop-event rule, not a new count of killers/assisters only.

Engagement E is the entire existing valid population. T = {e in E: min(cluster_blue,cluster_red)>=4}; N = E minus T (non-teamfight engagement, picks/skirmishes). T is a subset of E, not an unrelated category. Retain fine pick<=1/skirmish2..3/teamfight>=4 diagnostic columns. Preserve source negative-count convention only when provenance proves -1 encodes zero through int(count or -1); genuine unknowns flagged and excluded from T/N comparisons, retained in pooled E baseline. Report counts and identities: T intersect N empty, union equals known-scale E. Do not assume old market_event labelled109829 teamfights equals new deltaV population count.

Recover main scale columns from all32 corpus_shards_v33 shards by exact (groups/match,engage_ts=s) key. Do not import old y or X. Verify one-to-one joins, all566452 parent exposures and566104 valid rows; never silently retain only old market_event label-eligible rows. If shards omit cases, recompute counts from frozen detector only for missing matches and prove exact (s,L) identity. For external sets use already-adapted caches and same detector to regenerate participation fields and verify exposure keys/endpoints bitwise. Re-run detector checks on TRAIN-only fixtures. Persist cohort manifest containing keys, counts, source and rules before fitting.

Fit actual independent q pools on T and N (all eligible TRAIN rows in each); keep pooled E original frozen model as reference. Use existing full-run q candidates/parameters/weights/calibration and Q_CAL/Q_SELECT selection rules unchanged. Raw/sigmoid/isotonic; do not repair known isotonic extremes based on test. Train h90/h60/h120, select separately on each cohort's Q_SELECT, freeze all before new TEST scores. Scaler/vocabulary fit within cohort TRAIN only. Class membership/counts/duration/post outcomes never enter q features. Same-patch training of T/N can share matches since rows differ, but every row from each match keeps the same outer and validation roles.

Evaluate original pooled q and specialist q on identical T test rows, and on identical N test rows, with equal match weights per cell. This isolates specialization. Overall original pooled predictions versus retrospectively routed specialist predictions may be reported only as oracle-cohort routing, NOT a deployable live model; membership requires post-cutoff events. Paired match bootstrap1000 AUC/Brier/logloss differences, calibration and exact0/1 failures. Each external set separately, no test-fitting/selection. Also report picks/skirmishes inside N. Do not compare performance on different cohorts as an improvement from training.

## B. Prior role handling and causal role representation

Previous pipeline core/roles.py get_role_slots_from_detail used post-match teamPosition then individualPosition and participant-ID fallback, sorting bTOP,bJNG,bMID,bBOT,bSUP,rTOP,... . V2 deliberately removed this: current states sort team then participant ID; participant_slot names DO NOT assert lane. docs/CLAUDE_CURRENT_ALIGNMENT_AND_NEXT_STEPS_20260914.md and tests/test_state_value_v2_contract.py document role-metadata perturbation invariance. Preserve that contract and V labels. No post-match role directly as q input, no pretending raw slot1 is TOP. This new role experiment is ours, not a revival of the old sorting.

Use a small auxiliary role classifier whose inference reads only pregame-known champion ID and two selected summoner spell IDs (unordered spells encoded as categorical multi-hot; champion categorical). These fields exist in cache meta.static_meta.champion_by_pid and summoner_spells_by_pid, and external raw/adapted metadata. Preserve missing/unknown flags and all-zero unknown categories. No position interpolation, final damage/CS/items, player identity, W, future teamfight participation, or postgame role enters classifier predictors. This is estimated meta role, not observed current spatial lane; flex picks/lane swaps remain uncertain.

Role supervision comes only from15.14 TRAIN. First locate original raw detail files using project-configured paths, use valid teamPosition (or individualPosition fallback clearly marked) per participant. Missing/duplicate/invalid role annotations: exclude that team from role-supervision fit, not q cohort. If full main raw detail is unavailable, cached role_slots may be used ONLY as an explicitly weak role annotation, because core/roles.py can silently fill by ID. Audit available raw matches for fallback rate/agreement; write provenance before fitting. Do not call cached slots human truth or claim validated role accuracy from agreement with the same proxy. If both sources absent/unusable, finish A and report B blocked without inventing labels. External raw annotations available for final diagnostic role evaluation only after models frozen. W never used for role learning.

Classifier fixed OneHotEncoder(handle_unknown=ignore) -> multinomial LogisticRegression C1, max_iter2000, solver lbfgs, categorical champion and spell indicators, all eligible TRAIN teams, one row per participant with equal match weight. Fit fresh final role model on TRAIN, plus5 OOF models on same existing TRAIN folds (both teams and all queries from held-out match absent from their generating role model/preprocessing). No tuning or role calibration with TEST; fixed C is our engineering choice. For validation/external use final role model; TRAIN q role representation uses held-out-fold role outputs. Save source memberships. Check convergence; if max_iter insufficient increase cap only and log, do not tune hyperparameters from performance.

Given five participant role probabilities in each team, enumerate120 one-to-one role assignments pi, weight proportional to product_i p_i(role=pi_i), normalize with logsumexp, and marginalize to weights w_i,r. Clip probabilities at1e-12 only for numeric logs. This imposes standard one-of-each-role composition as a modeling assumption; report it, do not claim it holds for every team. Save posterior assignment entropy, unknown champion/spell flags and per-player marginal confidence. Never hard-route an ambiguous champion without recording uncertainty. Row/column sums must be1, all unseen symmetric inputs yield uniform marginal roles, permutation equivariance must hold. Fixed classifier/120-assignment construction is an experimental design choice.

## C. Learn role-specific influence without manually fixing lane values

Build role-aware q representation from each existing pre state: for every non-champion participant feature f, z[team,role,f] = sum_i w_i,role * x[participant_i,f]. Keep existing global/objective/time/p_pre features. Replace participant-slot block with role block (do not duplicate it for this arm). Add blue-minus-red differences for each corresponding role feature and difference*time_minutes for totalGold_norm,xp_norm,level_norm only. Add role-assignment uncertainty/missingness summaries. No observed fight participant list is a predictor. All quantities known by q_pre; role outputs only depend on draft fields. These weights represent role uncertainty, NOT lane win importance. The downstream q learns how role-specific leads and survival relate to deltaV label. No hand-set MID/ADC importance and no fixed lane reward in label.

For h90 fit on T and N a controlled comparison:
1. Existing participant representation: numeric ridgeC.01 and full-feature LightGBM (same250trees/15leaves/lr.04/minchild100/lambda1/colsample.9,seeds7/42/123 as existing economic model, now all allowed numeric pre features). This full-feature tree baseline is new and must be identified as such.
2. Draft-information control: original participant representation plus per-participant categorical champion/spell indicators (fit-only encoding). Same ridge and tree parameters. This distinguishes added draft information from role reorganization.
3. Role representation above, same ridge/tree budgets. Champion/spell direct indicators not duplicated here; they are used through role estimates. Feature sets differ explicitly, no claim that only parameter count changes.
Fit raw/sigmoid/isotonic calibrations on cohort Q_CAL, select within each representation on cohort Q_SELECT Brier/logloss/name; select overall h90 candidate including unchanged A specialist by same rule before test. Save each arm for paired comparisons even if not chosen. Freeze all choices before computing tests. Role arm optional blocked only for missing legitimate supervision, not because results disappoint. Separate A's specialization comparison from C's feature comparisons. No tuning based on a test improvement. If role arm loses, retain baseline and report it.

Role importance: for each cohort role-arm winner, report exact final-probability group Shapley over5 role groups (blue/red features,differences,phase interactions per role), one global-context group and one role-uncertainty group:7 groups128 coalitions. Deterministic up to256 TEST cases per cohort,128 cohort TRAIN background cases, hash-selected. If computationally large batch bounded rows, never silently replace SHAP with gain importance. Validate sum phi+base=actual selected calibrated prediction, reload identity. Mark this explains the role model even if overall winner is a different arm. Role ambiguity and correlated/derived features make interventional explanations descriptive, not causal. Calculate global mean absolute group contributions, signed local examples and time bands with N; simple bootstrap of explanation sample uncertainty and clear limited sample coverage. Explain champion role estimates and confidence in each case.

To substantiate additional predictive information, fit five leave-one-role-block-out versions of the preselected role ridge arm on T only, same TRAIN/Q_CAL/Q_SELECT rules, frozen before TEST. Role block includes its feature/difference/interactions; global context remains, so residual role information can remain. Report paired test Brier/logloss degradation with1000 match bootstrap and explicitly note redundancy. This is a model ablation, not causal importance. Do not perform post-test retraining for ablations.

## D. Tests, evidence, report and completion

Before full fits run TRAIN-only contract fixtures proving exact key joins/cohort partition, no cohort/postgame-role columns in predictors, role model OOF self-match exclusion, role inference invariance to changing/deleting queried match postgame roles/future events/W, static draft availability, permutation equivariance, probability normalization, schema/save-load identity, unchanged V labels. Explicitly guard against inherited unfiltered FULL/TEST smoke. All new model/ablation selections must predate new evaluation; existing tests have known exposure. Retain test metrics even if unfavorable.

Record counts by patch,cohort,role-label provenance; input/time/label definitions; hypothesis/choice versus evidence; before/after comparisons with identical test keys; model hashes; objective/staleness/isotonic limits; role reliability on independent annotations; missing/unseen champions; fine-scale and presence sensitivity as diagnostics (do not change main cut4). Update status.json regularly, report stages as implemented/running/executed/verified, save final REPORT.md and DEFINITION_AND_EVIDENCE.md. No completion from a PID. Finish A even if B blocked. Do not overwrite old manuscript with unreviewed new claims.

Verified literature for report, keep scope exact:
- Maymin2021, Smart kills and worthless deaths, DOI10.1515/jqas-2019-0096, https://d-nb.info/1367424143/34 : Tables6/7 analyze role-specific associations of performance bundles with match outcomes. Supports role-aware analysis motivation, NOT this role classifier, causal effects,90s horizon or predicted gains.
- Lee & Ramler2017, Identifying and Evaluating Successful Non-meta Strategies in League of Legends, FDG, DOI10.1145/3102071.3102081, author PDF https://myslu.stlawu.edu/~clee/docs/lol.team.composition.fdg.2017.pdf : role/composition inference using spells AND end-of-match item attributes. Supports role inference as a research problem; its end-of-match items are NOT allowed here and our classifier is NOT a reproduction.
- Lundberg & Lee2017 SHAP, https://arxiv.org/abs/1705.07874 : feature attribution framework, not causal lane importance; our grouping/background budget is operational.
- Cut4 and participation rules: local current v3.3 manifest/manuscript; convention with reported cut3/4/5 sensitivity, not a universal threshold established by a paper. Preserve prior pooled-definition test exposure disclosure.
