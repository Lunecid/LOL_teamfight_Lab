# Corrected full-corpus preflight — no training

User corrected the scope: entire 210,000-match corpus, 15.14 TRAIN,15.15 VALIDATION,15.16 TEST, plus latest KR and other-region external TEST. Old P1–P4 are pilots, not the requested full experiment. Read docs/FULL_CORPUS_SPLIT_CORRECTION_20260915.md and docs/EXTERNAL_TEST_PLAN_20260915.md first. Codex designs; Opus5 implements/runs. Use TEAMFIGHT_WORKSPACE; Python C:/Users/todtj/anaconda3/python.exe. Deadline2026-09-15 08:10KST; no new stage after that, checkpoint safely. CPU<=4. No installs/deletes/secrets/commits, original/GPU queue/frozen artifacts unchanged.

Implement and RUN an inventory/preflight only, writing new scripts and outputs/full_corpus_preflight_20260915. Do not train, score models, create labels, select hyperparameters or inspect final winners beyond recording availability. Goal is exact matching manifests and evidence, not a promise to do it later.

1. Inspect D:/LOL_Project/cache/match_cache_fresh_v3_engage_status13 metadata to enumerate all match IDs/patches and required npz/meta/events file existence. Expected prior census: 15.14=74673,15.15=74748,15.16=60579, total210000. Verify actual counts, unique IDs, malformed/missing inputs; do not sample or substitute the 50k pilot. Assign patch roles exactly. Metadata-only/full-header checks are acceptable if explicitly scoped; do not claim full semantic content validation from file existence.
2. Join parent exposures outputs/postkill_objective_delay_full/exposures.csv to manifest, count detected engagements, match coverage, overlapping next_start<=L exclusions and unknown/missing patches per role. Do not re-detect or score labels; preserve original definition. Report raw-match counts separately from eligible engagement counts. Unknown discrepancy must be reported, never silently dropped.
3. Read only SQLite collector DBs listed in outputs/external_test_inventory_20260915.json with mode=ro. Enumerate complete queue420/map11 matches; match detail/timeline filenames, verify DB-recorded hashes against bytes where feasible within deadline, collect actual gameVersion prefix/region/game_creation without using winners or predicted performance. Target sets KR16.13 10064,KR16.15 926,KR16.14 pilot200,NA1 16.13 10000; EUW complete0. Never include pending/excluded. Preserve raw API patch string and folder identity; do not guess16.xx/26.xx mapping. If DB includes other new completed patches, record but do not silently expand predefined test membership.
4. Check match ID overlap across all train/val/test/external sets and file aliases; exact ID dedup without conflating regions. Build per-region/per-patch manifests, exclusion counts and hashes. Mark KR16.13 previously used, KR16.14 pilot provenance, other prior-use unknown unless reliable evidence is found. 'Unknown prior use' is not 'untouched'. Existing local logs/docs may be searched for exact datasets but do not read unrelated session histories/secrets.
5. Record runtime schemas/available files sufficient to design a full pipeline. Explicitly state that existing frozen V trained on a mixed-patch pilot cannot be reused to label a clean15.16 TEST. TRAIN OOF V and VALIDATION calibration/selection dependency need a subsequent Codex specification; do not invent/execute cross-fitting here.

Write protocol/status/checkpoints, main_matches.csv, external manifests, coverage.json, validation.json, REPORT.md. Separate verified/unchecked/error. Log exact commands and source hashes. Monitor to completion or deadline; if deadline insufficient, retain partial manifest and explicit remaining counts. Do not label the full-model task complete. Return concise actual census and discrepancies.
