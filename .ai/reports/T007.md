# T007 보고서 — q 파이프라인 코호트 매개변수화 + T 회귀 검증

status: DONE  
base_branch_ref: `origin/claude/happy-heisenberg-h93ypi` (계약·`.ai/tasks` 반입)  
executed_on: `feature/fight-boundary-pipeline`  
executed_at: 2026-09-20 (Cursor)  
contract: `docs/SCALE_SPLIT_EXPERIMENT_CONTRACT_20260920.md` §6 준수 (S 실험 미실행)

## 목표 달성
2026-09-20 q 파이프라인 4개 스크립트에 `--cohort` / `--cohort-tag` / `--out-dir` / `--reuse-evaluators` / `--fixed-learner` / `--q-model`를 추가했다. **기본값(T)으로 frozen-eval 라벨을 임시 디렉터리에 재생성했을 때 동결 T 산출물과 바이트 동일**을 확인했다. 새 S 적합·평가는 하지 않았다.

## 변경 파일
| 파일 | 내용 |
|---|---|
| `scripts/rr20260920_q_build_newv_labels.py` | `cohort_keys(..., cohort)`; `--cohort {T,S}`; `--out-dir`; `labels_out_dir`; 메타에 `cohort`·마스크 규칙 |
| `scripts/rr20260920_q_train_oof_mlp_folds.py` | C1 `cohort_keys` import; `--cohort`; `--reuse-evaluators`; S 라벨 경로 + T `oof_evaluators` 고정; S census 경고; `role_tag` |
| `scripts/rr20260920_q_newv_primary_fit.py` | `--cohort-tag {T,S,TS}`; 경로 분기; `--fixed-learner logit_state`; docs 접미사 |
| `scripts/rr20260920_review_response_rr12.py` | `--cohort-tag {T,S}`; `--q-model`; OUT/docs 접미사; “all-T” → cohort명 |
| `.ai/reports/T007.md` | 본 보고서 |

**미변경:** V 번들·어댑터·특징 순서·모델링 격자; `outputs/q_newv_fit85_20260920/` 내부 파일; `docs/journal_manuscript_v1/`·잠금 문서.

## 검증 출력

### py_compile
```
COMPILE_OK
```

### 데이터 / fine 열
- data_root: `~/문서/LOL_Teamfight` (exists)
- `MAIN_TEST_cohort.npz` files include `fine` → **not BLOCKED**

### OOF evaluators (T008 전제)
```
oof_exists=True
bundle_oof_fold{0..4}.joblib
V_oof_fold{0..4}_mlp_expanded.joblib
```

### T 회귀 (`--out-dir %TEMP%/t007_regress`, 동결 경로 미기록)
```
TEST_h90.npz     sha_frozen 0a2e977b231acf85 sha_regen 0a2e977b231acf85 BYTE SAME
Q_CAL_h90.npz    sha_frozen 1890d2ba7970a093 sha_regen 1890d2ba7970a093 BYTE SAME
Q_SELECT_h90.npz sha_frozen 4f52aff6d252fe15 sha_regen 4f52aff6d252fe15 BYTE SAME
```
- RR0 manifest `digests.TEST_h90.sha16` = `0a2e977b231acf85` → **일치**
- 바이트 동일하므로 배열 단위 비교 불필요

### git / freeze 산출물
- 스크립트 4개 diff 존재
- `outputs/q_newv_fit85_20260920/` working-tree 변경 **없음**

## BLOCKED / 미실행
- 없음 (데이터·fine·T 회귀 통과)
- **S 라벨/적합/평가 미실행** (T008–T009)

## 후속
- T008: `--cohort S` frozen-eval 라벨 + `--reuse-evaluators` TRAIN OOF 점수
- T009: `--cohort-tag S` / `TS` + `--fixed-learner logit_state` + RR12 `--q-model`
