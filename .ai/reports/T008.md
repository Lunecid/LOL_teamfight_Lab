# T008 보고서 — skirmish(S) 라벨 생성 + 무결성 + RR0 manifest

status: DONE  
base_commit: `1229b1d` (`origin/claude/happy-heisenberg-h93ypi` ff-merge)  
executed_on: `feature/fight-boundary-pipeline`  
executed_at: 2026-09-20 (Cursor)  
contract: `docs/SCALE_SPLIT_EXPERIMENT_CONTRACT_20260920.md` §2–§3, §7  
manifest: `docs/SCALE_SPLIT_RR0_MANIFEST_20260920.json` (`PASS: true`)

## 목표 달성
S = `cohort==0 & fine==1` 행에 대해 T와 같은 평가기로 p_pre·ΔV̂·Y·B40을 생성했다. **새 모델 적합 없음.** T 동결 트리 `outputs/q_newv_fit85_20260920/`는 sha/mtime 스냅샷 대비 **무변경**.

## 실행
1. `oof_evaluators/` 10개 확인 (fold×{V_oof, bundle}).
2. `python scripts/rr20260920_q_build_newv_labels.py --cohort S --roles TEST Q_CAL Q_SELECT`
3. M2 assert + reuse `fold_matches`를 V-bucket으로 수정 후  
   `python scripts/rr20260920_q_train_oof_mlp_folds.py --cohort S --reuse-evaluators --folds 0,1,2,3,4`

로그: `.ai/reports/logs/T008_build_S_labels.txt`, `T008_train_oof_S.txt`, `T008_fold_set_compare.txt`.

## M2 (T007 검토) — fold별 `len(fold_matches)` vs `n_held_matches`
| fold | len(fold_matches) | n_held_matches | equal |
|---|---:|---:|---|
| 0 | 14628 | 14628 | ✓ |
| 1 | 15017 | 15017 | ✓ |
| 2 | 14796 | 14796 | ✓ |
| 3 | 14769 | 14769 | ✓ |
| 4 | 14958 | 14958 | ✓ |

**발견:** T007 reuse 경로가 eng-role 경기 집합을 쓰면 fold0 기준 13625 vs 14628로 M2가 즉시 실패한다. eng ⊂ V-held (`only_eng=0`, fold당 V-only ≈981–1054). leave-match는 eng⊂V-held라 안전하지만 stray 검사가 자기참조였다.  
**조치:** reuse 모드에서 적합 없이 V-bucket fold 경기 집합을 로드하도록 수정 + M2 assert 유지. (수용 기준의 “스크립트 변경 없음”과 충돌 → 필수 수정으로 기록.)

## 산출물 (S)
`outputs/q_newv_fit85_20260920_S/labels/`
| 파일 | n | sha16 |
|---|---:|---|
| TEST_h90.npz | 101205 | `dbf19eb4ea441128` |
| Q_CAL_h90.npz | 31302 | (manifest digests) |
| Q_SELECT_h90.npz | 31059 | (manifest digests) |
| TRAIN_oof_h90.npz | 123049 | `9b17363b36364d8d` |

TRAIN fold n = 24030 / 25005 / 24520 / 24690 / 24804 — **census 완전 일치** (유한 점수 제외 0).

## 무결성 (모두 PASS)
| 검사 | 결과 |
|---|---|
| 행 수 vs census | PASS (차이 0) |
| 키 분리 S∩T (match,s) | PASS (교집합 0) |
| fold 분리: S·T 경기 ⊆ V-held; \|V-held\|=n_held | PASS |
| Y==(ΔV>0), ΔV≈p_post−p_pre (atol 1e-7), B40 from p_pre | PASS |
| n_exact_zero | TEST 0, Q_CAL 0, Q_SELECT 1, TRAIN 1 |
| P(SVI=1) 경기가중 | TEST 0.5107, Q_CAL 0.5062, Q_SELECT 0.5139, TRAIN 0.4982 |
| valid_h90==1 전부 | PASS (`pre_ok` 열은 라벨 시점에 필터되어 부재) |
| T freeze untouched | PASS (19파일 스냅샷 불변) |

## 변경 파일 (git)
| 파일 | 이유 |
|---|---|
| `scripts/rr20260920_q_train_oof_mlp_folds.py` | M2 assert + reuse `fold_matches`←V-bucket |
| `docs/SCALE_SPLIT_RR0_MANIFEST_20260920.json` | 입력/출력 sha16·무결성 |
| `.ai/reports/T008.md` | 본 보고서 |
| `.ai/reports/logs/T008_*` | 실행·비교 로그 |

**미변경:** fit85 번들·fold joblib 바이트, T labels, 원고·lock 문서. q 적합·RR12 미실행 (T009).

## BLOCKED
없음.

## 후속
T009: `--cohort-tag S` / `TS` + `--fixed-learner logit_state` + RR12 (R1 `--out-suffix` 가드 선행).
