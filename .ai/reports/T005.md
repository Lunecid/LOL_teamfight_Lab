# T005 보고서 — `\pending` 3건 해소 (endpoint 규칙·코호트 규칙·경기 수)

status: DONE
base_commit_checked: b5903c3
executed_at: 2026-09-20 밤 (Claude 세션이 직접 실행)

## 변경 파일
- docs/lineage_20260915/ (신규 7파일: README + 사본 6개; 사본 sha256 = `git show e0ec3d0:<path>` 와 동일, README 표에 sha256[:16] 기록)
- docs/journal_manuscript_v1/02_METHODS_CORE.md: §1 코호트 규칙 문장(`\pending{T-cohort-build-provenance}` 제거), §1b endpoint 규칙 전면 교체(`\pending{h90-endpoint-rule}` 제거; 수식·유효 조건 6개·무효 처리·348행·실현 통계; "L_i = e_h − q_pre" 문장을 상한 앵커와 구분해 정정), §5 표 5b(TRAIN 28 989 Σ·Q_CAL 7 550·Q_SELECT 7 469·V_CAL/V_SELECT 행·EXT T 경기 수)와 pooled 문장(`\pending{pooled-T-matches}` 제거; 83 108을 "manifest 필드의 합"으로 명시), §9 근거 행 5개 추가
- docs/PAPER_COHORT_CONTRACT_20260919.md §2 주석에 V_CAL/V_SELECT 구절 추가(추가만)
- docs/journal_manuscript_v1/README.md Change log(T005·T006), .ai/CONTEXT.md("없는 것" 절 갱신)

## 검증
```
사본 6개 sha256 비교                         → 전부 SAME
\pending{...} 마커                            → 0 (grep -c '\pending' = 1은 §8 "prior deep-learner `\pending` comparisons" 단어 언급)
새 문장 수치·조건 39건 ↔ lineage 사본/tex     → missing 0
cohort_manifest fold T_matches 합             → 28 989; pooled 합 83 108
git diff --numstat PAPER_COHORT_CONTRACT      → 삭제 0
```

## 계획과 달라진 점
- T의 자격 규칙은 sec_definition에 있었으므로 `\pending{T-rule}`은 처음부터 불필요했고, 이번에 생성 코드(`scale_classes`)와 manifest `rules`로 provenance를 닫음.
- pooled T 경기 수는 "기록된 필드의 합"이라는 단서와 함께 기재(계획대로). 저자가 원치 않으면 그 문장만 삭제.
- 실현 endpoint 통계(45.493 s 등)는 구 로지스틱 V 실행에서 계산된 것이나 endpoint 시각 자체는 V와 무관 — README에 이 단서를 적음.

## BLOCKED / 미실행
- 없음.
