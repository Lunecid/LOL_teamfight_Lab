# T010 — 계약 문언(identity) 재계산 + T009 문서 수정 (R3·R4·R5·G2·M3·M4)

approval: approved (2026-09-21)
base_commit: `aa2249f` (T009 tip)
계약: `docs/SCALE_SPLIT_EXPERIMENT_CONTRACT_20260920.md` §4–§6
선행: T009 (검토 `.ai/reports/T009_review.md` 조건부 통과) · 후속: 저자의 저널 포함 결정(계약 §8)

## 목표
S arms를 계약 §4 문언대로 **전 모델 identity calibrator**로 재평가해 주 결과로 두고, T009의 RR12 2단계 선택 값은 민감도로 병기한다. 재적합(primary_fit) 없음. T 동결 산출물·문서 무변경.

## 변경하지 않을 것
- `outputs/q_newv_fit85_20260920{,_S,_TS}/models/*`(읽기만), 동결 T 문서 3개, 원고, 잠금 문서, 계약 본문(저자가 (b)를 택하면 별도 개정 이력만 추가).
- T009 산출물 디렉터리(`_qS`/`_qT`/`_qTS`/`_qTS_on_T`)는 삭제·덮어쓰기 없음(민감도 변형으로 유지).

## 실행 순서
```bash
# 0) rr12: --calibrator {select,identity} 추가 (기본 select = 현 동작, T 불변).
#    identity면 2단계를 건너뛰고 전 모델 identity; JSON/md에 calibrator_policy 기록; q_RR 별칭 대신 q_base로 표기.
python -m py_compile scripts/rr20260920_review_response_rr12.py
# 1) S 3회 재실행 (identity)
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --calibrator identity --q-model outputs/q_newv_fit85_20260920_S/models/logit_state.joblib  --out-suffix _qS_id
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --calibrator identity --q-model outputs/q_newv_fit85_20260920/models/logit_state.joblib    --out-suffix _qT_id
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --calibrator identity --q-model outputs/q_newv_fit85_20260920_TS/models/logit_state.joblib --out-suffix _qTS_id
#    _qTS_on_T는 이미 identity(동결 T 절차와 동일)이므로 재실행 없음.
# 2) 페어 대비: ss20260920_paired_contrasts.py에 --variant {sel,id} (입력 접미사·출력 파일명 매개변수화; 기본 sel = 현 동작)
python scripts/ss20260920_paired_contrasts.py --variant id      # → outputs/scale_split_TvsS_20260920/paired_contrasts_id.json
# 3) rr12 L825 "B40: small exploratory support (CI near 0)…" 를 B40 CI가 0을 포함할 때만 출력하도록 조건화(R5) → S 문서 6쌍(_qS/_qT/_qTS, _qS_id/_qT_id/_qTS_id) 재생성 확인
# 4) 결과 문서 재작성: ss20260920_write_results.py
#    - 표 1: identity(raw) 값을 본표로, sigmoid 값은 "표 1b (RR12 2단계 선택 변형)"; lgbm 행은 primary_table.json의 TEST 블록(n 101 205)으로(R4)
#    - 표 2a "계약 문언(identity) — primary 1행" / 표 2b "RR12 2단계 선택 변형(민감도)"; 두 표 모두 estimate·CI·p_gt0·n·matches·seed
#    - 표 3 CORP: identity 기준(q_S raw, PT_flex_S raw); sigmoid 값은 2b 옆에
#    - 표 4 EXT S: 변경 없음(rrx는 raw)
#    - md·json 모두 같은 실행에서 source_commit 기록(M3)
python scripts/ss20260920_write_results.py
# 5) manifest 생성기: RR0 동결 digest 대조(G2) 추가 → T_frozen_rr0_digests_match; digests_T009에 _id 산출물·paired_contrasts_id 추가
python scripts/ss20260920_scale_split_manifest.py --check-integrity --with-t009
# 6) docs/Q_NEWV_FIT85_PRIMARY_20260920_{S,TS}.md 상단 배너 1줄(M4; 추가만)
```

## G2 대조 목록 (RR0 manifest `docs/REVIEW_RESPONSE_RR0_MANIFEST_20260920.json` `digests`)
`logit_state d6e8fc31310a5a58` · `primary_table a80340fede7b6e6a` · `selection_freeze 294a3e4261f9a40e` · `rr12_paired_ci 9484c09c4e097f25` · `rr12_prediction_table b16b1ce417d61f53` · `rr12_PT_flex 15acc630abe5930d` · `rr12_PT_linear 2215150ca4bc0f89` · `rr12_b_spline b34f46c62d72637b` · `rr12_baseline_selection 4b798eed3795e789`. 하나라도 불일치 → BLOCKED.

## 검증
```bash
python -c "import json; m=json.load(open('docs/SCALE_SPLIT_RR0_MANIFEST_20260920.json')); print(m['T_frozen_rr0_digests_match'], m['integrity']['PASS'], m['generator'])"
grep -c 'CI near 0' docs/REVIEW_RESPONSE_RR12_RESULTS_20260920_S_*.md      # 모든 파일 0
grep -n 'primary' docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md                 # 표 2a에 1행만 (2b는 'sensitivity')
grep -nE 'scale gradient|more predictable|routing|deploy' docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md   # Forbidden 절 안에만
grep -n 'lgbm' docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md                    # n=101205
git diff --stat -- docs/REVIEW_RESPONSE_RR12_RESULTS_20260920.md docs/REVIEW_RESPONSE_RR12_RESULTS_20260920.json docs/REVIEW_RESPONSE_RRX_EXTERNAL_20260920.md docs/journal_manuscript_v1   # 비어 있음
python - <<'PY'   # 결과 md 수치 ↔ JSON 필드 존재 확인 (T002 §9 방식)
PY
```

## 수용 기준
- 표 2a(identity) primary 1행; 표 2b(sigmoid 변형) 5행 병기; 두 변형의 주 대비 부호 동일 여부를 보고서에 명시(다르면 BLOCKED, 저자 결정).
- q_T→S raw(0.24775 근방)가 표 1에 있음; q_S − q_T→S identity 쌍 CI 보고.
- `T_frozen_rr0_digests_match: true`; T008 integrity 블록 불변; 동결 T 문서 3개 diff 0.
- `.ai/reports/T010.md`에 R3~R5·G2·M3·M4 각각의 명령·출력 기록.

## 중단 조건
- G2 digest 불일치 → BLOCKED.
- identity 변형에서 주 대비 부호가 바뀌면 → BLOCKED(저자 결정; 두 변형 모두 보고).
- 계약 밖 arm·필터가 필요해 보이면 실행하지 않고 "제안" 절에만.
