# T015 — G0 Phase A: 실행 무결성 감사 (코드·단위 테스트·상태 문서)

approval: approved (저자 "진행해보도록", 2026-09-21)
base_commit: `55982c6`
계약: [`docs/SUPPLEMENTARY_EXPERIMENT_DESIGN_20260921.md`](../docs/SUPPLEMENTARY_EXPERIMENT_DESIGN_20260921.md) §3  
선행: `cursor/q-provenance-repro-fixes` 병합 (해시·S reuse·join 가드·`tests/test_q_provenance_fixes.py`)  
후속: G0 Phase B (outputs 필요: 헤드라인 재집계·fold 집합 해시·번들 reload·미래정보 불변성) — **별도 작업서**

## 목표

보완설계 G0의 **데이터 비의존 부분**을 감사·문서화하고, S reuse 시 T OOF 디렉터리 **쓰기 가드**를 보강한다.  
새 실험·재적합·헤드라인 수치 교체 **금지**. `outputs/`·대형 NPZ/joblib **열지 않음** (`AGENTS.md`).

## G0 항목 매핑 (이번 작업)

| ID | 설계 §3.1 | Phase A | Phase B (이번 제외) |
|---|---|---|---|
| G0.1 | commit·모듈·환경 기록 | 감사 스크립트가 JSON에 기록 | import된 실제 .py 바이트 해시(런타임) |
| G0.2 | 최종 저장 후 평가기 SHA | 단위 테스트로 함수 검증 | 디스크상 동결 번들 재해시 |
| G0.3 | S reuse 강제 + T 경로 쓰기 실패 | reuse 가드 유지 + **쓰기 가드 추가** | 실경로 E2E |
| G0.4 | (match,s) join 누락/중복 | 단위 테스트 | 실라벨 전수 |
| G0.5 | fold held 경기 집합 해시 | 코드 경로 존재 확인만 | outputs 경기 집합 대조 |
| G0.6 | 번들 reload parity | 상태 INCOMPLETE | joblib 재로드 vs 저장 예측 |
| G0.7 | 헤드라인 재집계 | INCOMPLETE (outputs 금지) | 동결 가중치로 docs 수치 재현 |
| G0.8 | 데이터 부족 시 incomplete | 감사 스크립트 incomplete 필드 | — |
| G0.F | 미래정보 불변성 §3.2 | INCOMPLETE | X_pre 재구성 실험 |

## 변경 범위

### 편집
1. `scripts/rr20260920_q_train_oof_mlp_folds.py` — S+reuse일 때 OOF_DIR_T에 evaluator/bundle **dump 금지** 가드 함수 + dump 직전 호출.
2. `tests/test_q_provenance_fixes.py` — 쓰기 가드 단위 테스트.
3. `scripts/g0_integrity_audit.py` — G0 상태 감사 (repo 전용, outputs 미개방).
4. `docs/G0_INTEGRITY_STATUS_20260921.{json,md}` — 판정표.
5. `.ai/reports/T015.md` — 보고서.
6. `docs/master_thesis_v1/00_INVENTORY_AND_STATUS.md` — G0 행을 Phase A 결과로 갱신 (배너/상태만).

### 변경하지 않을 것
- 저널 원고 수치, LOCK/CONTRACT 수치 행, `docs/tog_manuscript/`, 동결 모델, 예측표 재생성.
- E1–E6 실험 실행.

## 검증
```bash
python -m pytest tests/test_q_provenance_fixes.py -q
python scripts/g0_integrity_audit.py --out-json docs/G0_INTEGRITY_STATUS_20260921.json --out-md docs/G0_INTEGRITY_STATUS_20260921.md
python -m py_compile scripts/rr20260920_q_train_oof_mlp_folds.py scripts/g0_integrity_audit.py
```

## 수용 기준
- provenance 테스트 전부.
- `G0_INTEGRITY_STATUS`에 G0.1–G0.8·G0.F 각각 `PASS` / `PARTIAL` / `INCOMPLETE`와 근거.
- Phase B 항목은 incomplete이며 FAIL 데이터를 정상 산출물로 게시하지 않음.
- 새 헤드라인 수치 없음.

## 중단 조건
- outputs를 열어야만 끝나는 검사를 Phase A에서 강제 → 그 항목 INCOMPLETE로 남기고 중단하지 말고 보고.
- 쓰기 가드가 T 정상 fit 경로를 깨뜨리면 BLOCKED 후 롤백.
