# T008 — skirmish(S) 라벨 생성 + 무결성 + RR0 manifest

approval: approved (2026-09-20)
base_commit: T007 완료 커밋
계약: `docs/SCALE_SPLIT_EXPERIMENT_CONTRACT_20260920.md` §2–§3, §7
선행: T007 (기본값 회귀 통과, fold 평가기 존재 확인) · 후속: T009

## 목표
S = `cohort==0 & fine==1` 행의 p_pre·ΔV̂·Y·B40을 T와 같은 평가기로 생성한다. 새 모델 적합 없음.

## 변경하지 않을 것
- `outputs/q_newv_fit85_20260920/`(T) 안의 어떤 파일도.
- 평가기(fit85 번들, fold joblib), 정의·h90·결측 규칙.

## 실행
```bash
# 1) 전제 확인
ls outputs/q_newv_fit85_20260920/oof_evaluators/            # 10개 파일(fold 5 × {V_oof, bundle})
# 2) frozen-eval 역할
python scripts/rr20260920_q_build_newv_labels.py --cohort S --roles TEST Q_CAL Q_SELECT
# 3) TRAIN OOF (fold 평가기 재사용)
python scripts/rr20260920_q_train_oof_mlp_folds.py --cohort S --reuse-evaluators --folds 0,1,2,3,4
```
fold joblib 또는 bundle이 없으면 **여기서 멈추고** 보고서에 BLOCKED. 계약 §3의 refit fallback(T·S 재라벨링 + T 재현율)은 저자가 보고서를 보고 승인한 뒤 별도 작업서로.

## 무결성 검사 (스크립트로 실행, 결과를 manifest에 기록)
- 행 수: TEST 101 205, Q_CAL 31 302, Q_SELECT 31 059, TRAIN fold0–4 24 030 / 25 005 / 24 520 / 24 690 / 24 804 (유한 점수 제외로 소수 차이 허용; 차이 수·사유 기록).
- 키 분리: S 라벨의 (match, s) 집합 ∩ T 라벨 집합 = ∅ (같은 경기의 다른 교전은 허용, 같은 교전은 불허).
- fold 분리: 각 fold k의 S 경기 집합이 T OOF 메타의 fold k 경기 집합과 일치(같은 해시); 어떤 S 행도 자기 경기가 학습에 포함된 V로 점수 매겨지지 않음.
- 값 일관성: `Y == (delta > 0)`, `delta == p_post − p_pre`, `B40 == (0.40 ≤ p_pre ≤ 0.60)`, `n_exact_zero` 수, 양성률(경기 가중) 기록.
- 유효성: 모든 행 `valid_h90 == 1 & pre_ok == 1`.

## 산출물
- `outputs/q_newv_fit85_20260920_S/labels/{TRAIN_oof_h90.npz, TEST_h90.npz, Q_CAL_h90.npz, Q_SELECT_h90.npz}` + 메타.
- `docs/SCALE_SPLIT_RR0_MANIFEST_20260920.json`: 입력 파일(코호트 npz, 라벨 npz, fit85 번들, fold joblib) sha16, 출력 sha16, 행 수·경기 수, 위 검사 결과(PASS/FAIL), `role_tag`, 실행 시각·커밋.
- `.ai/reports/T008.md`.

## 수용 기준
- 모든 무결성 검사 PASS; manifest 작성; `git diff --name-only` = manifest + 보고서(스크립트 변경 없음).

## 중단 조건
- fold 평가기 부재 → BLOCKED(위).
- fold 경기 집합 불일치 또는 키 중복 → BLOCKED.
