# T016 — G0 Phase B: 동결 산출물 digest·헤드라인 재집계·OOF reload (repo outputs)

approval: approved (저자 "다음 진행", 2026-09-21)
base_commit: `0a8cb2f` (T015)
계약: [`docs/SUPPLEMENTARY_EXPERIMENT_DESIGN_20260921.md`](../docs/SUPPLEMENTARY_EXPERIMENT_DESIGN_20260921.md) §3  
선행: T015 Phase A (`PARTIAL_PHASE_A`)
후속: E3 외부 bootstrap (행 단위 EXT 예측 dump 필요 시 별도) / E1

## 목표

repo `outputs/`에 이미 있는 **동결** 산출물만 읽어 G0.2·G0.6(부분)·G0.7을 검증한다.  
재적합·재선정·헤드라인 교체·LOCK 수치 행 수정 **금지**. 외부 코퍼스 재점수 **없음** (E3는 다음).

## 허용하는 outputs 읽기 (명시 예외)

| 경로 | 용도 |
|---|---|
| `outputs/review_response_rr12_20260920/{prediction_table.npz,paired_ci.json,baseline_selection.json,models/*}` | RR0 digest + ΔBrier 재집계 |
| `outputs/review_response_rr12_20260920_S_qS_id/prediction_table.npz` | S identity ΔBrier 재집계 |
| `outputs/q_newv_fit85_20260920/{models/logit_state.joblib,primary_table.json,selection_freeze.json,labels/TEST_h90.npz,labels/TRAIN_oof_*.json}` | RR0 digest |
| `outputs/q_newv_fit85_20260920/oof_evaluators/V_oof_fold*.joblib` (+ bundle) | 최종 SHA 기록 + load 키 검사 |

열지 않음: `data/raw/`, 원본 매치 JSON, EXT 재점수용 대형 engagement 테이블(없으면 INCOMPLETE).

## 실행
1. `scripts/g0_phase_b_verify.py` — digest 대조, T/S 경기가중 ΔBrier 재집계, OOF load+sha, 상태 JSON/MD 갱신.
2. `docs/G0_INTEGRITY_STATUS_20260921.*`에 Phase B 블록 병합 (기존 Phase A 유지).
3. `.ai/reports/T016.md`.

## 수용 기준
- RR0 `digests` 중 존재하는 경로 **전부 MATCH** (불일치 → BLOCKED, 헤드라인 수정 금지·보고만).
- T: ΔBrier(q−PT_flex) ≈ −0.003732 (docs −0.00373); n=32981 / matches=24020.
- S identity: ≈ −0.003353 (docs −0.00335); n=101205 / matches=49730.
- OOF fold0 `n_eng_labeled` finite; sha16 기록.
- 새 실험 수치·원고 헤드라인 변경 0.

## 중단 조건
- digest DIFF → BLOCKED (원인 조사만; 재적합 금지).
- 필수 파일 부재 → 해당 항목 INCOMPLETE, 나머지 진행.
