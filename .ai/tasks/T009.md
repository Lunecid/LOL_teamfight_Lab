# T009 — 적합·평가·보고: q_S / q_T→S / q_TS vs PT_flex_S

approval: approved (2026-09-20)
base_commit: T008 완료 커밋
계약: `docs/SCALE_SPLIT_EXPERIMENT_CONTRACT_20260920.md` §4–§7 (주 대비 1개, 금지 해석 그대로)
선행: T008 (manifest PASS)

## 목표
계약 §4의 arms를 적합하고 §5의 대비를 계산해 결과 보고서를 만든다. 학습기 재선정 없음, TEST 선택 없음.

## 변경하지 않을 것
- T 동결 산출물·`models/logit_state.joblib`(읽기만), 저널 원고, 잠금 문서.
- 계약에 없는 arm·대비·필터 추가(발견한 것은 보고서 "제안" 절에만).

## 실행 순서
**0) R1 먼저 (T007 검토):** `rr20260920_review_response_rr12.py`에 `--out-suffix`와 "non-default `--q-model`이면 `--out-suffix` 필수" 가드를 추가하고 기본값 불변을 확인한 뒤 아래를 실행한다. 아래 3)·4)의 각 호출에 `--out-suffix _qS` / `_qT` / `_qTS` / `_qTS_on_T`를 붙인다.

```bash
# 1) S 전용 적합 (logit_state 고정, PT_flex_S 선정, lgbm 진단)
python scripts/rr20260920_q_newv_primary_fit.py --cohort-tag S --fixed-learner logit_state --boot-reps 800
# 2) 합집합 라벨 생성(작은 보조 스크립트 scripts/ss20260920_union_labels.py: T와 S의 4개 라벨 npz를 (match,s) 기준 concat, 중복 0 확인, 메타 기록) 후 pooled 적합
python scripts/ss20260920_union_labels.py
python scripts/rr20260920_q_newv_primary_fit.py --cohort-tag TS --fixed-learner logit_state --boot-reps 800
# 3) 주·보조 대비 (S TEST 행; PT_flex_S 선정은 S Q_SELECT)
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --q-model outputs/q_newv_fit85_20260920_S/models/logit_state.joblib     # q_S
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --q-model outputs/q_newv_fit85_20260920/models/logit_state.joblib       # q_T→S (출력 접미사 _qT)
python scripts/rr20260920_review_response_rr12.py --cohort-tag S --q-model outputs/q_newv_fit85_20260920_TS/models/logit_state.joblib    # q_TS→S (접미사 _qTS)
# 4) T 행에서 q_TS vs q_T (동일 T 행, 동결 T PT_flex와 함께)
python scripts/rr20260920_review_response_rr12.py --cohort-tag T --q-model outputs/q_newv_fit85_20260920_TS/models/logit_state.joblib --out-suffix _qTS_on_T
# 5) 페어 대비: 보조 스크립트 scripts/ss20260920_paired_contrasts.py — 동일 S 행에서 (q_S − PT_flex_S)[주], (q_S − q_T→S), (q_S − q_TS); 동일 T 행에서 (q_TS − q_T); S∩B40; 경기 부트스트랩 2 000회 seed 7, w=1/n_m; CORP는 scripts/forecast_diagnostics.py 재사용(점값)
# 6) EXT S dual-stage: scripts/rr20260920_review_response_rrx_external.py의 경로를 S로 매개변수화(--cohort-tag S, S 코호트 마스크) 또는 소스크립트; CI 없음 명시
```
`--out-suffix`가 없으면 T007 스크립트에 추가(기본값 불변).

## 보고서 `docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md` (+ `.json`)
- 머리: role_tag, 계약 링크, 커밋, 실행 시각, "T 수치는 동결값 인용".
- 표 1 (S TEST): 모델 | n | matches | Brier | logloss | AUC — q_S, q_T→S, q_TS, PT_flex_S, PT_linear_S, b(p)_S, (lgbm_state_S: diagnostic).
- 표 2 대비: **primary** ΔBrier(q_S − PT_flex_S) CI · secondary (q_S − q_T→S), (q_S − q_TS), T 행 (q_TS − q_T), S∩B40 (q_S − PT_flex_S) — 각각 estimate·CI·p_gt0·n·matches·seed.
- 표 3 CORP(점값): q_S, PT_flex_S — MCB/DSC/UNC/ECE.
- 표 4 EXT S (CI 없음): cohort | n | V_pre Brier | ΔBrier(q_S − PT_flex_S) | ΔMCB | ΔDSC.
- "허용/금지 해석" 절: 계약 §6 그대로 복사. "제안" 절: 발견한 후속 질문(실행하지 않음).
- 모든 수치는 JSON 산출물 필드에서 옮기고, 각 표 아래에 파일·필드 각주.

## 검증
```bash
grep -cE 'scale gradient|more predictable|routing|deploy' docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md   # 0 (금지 절의 인용 제외 — 인용은 "Forbidden" 절 안에만)
grep -c 'primary' docs/SCALE_SPLIT_TvsS_RESULTS_20260920.md   # 주 대비 표기는 1행
python - <<'PY'   # 보고서 수치 ↔ JSON 필드 존재 확인 (T002 §9 방식)
PY
git diff --name-only   # 새 스크립트 2–3개 + 보고서 + manifest 갱신 + .ai/reports/T009.md; T 동결 산출물 변경 없음
```

## 수용 기준
- 표 1–4 모두 채워짐(EXT는 소코호트 "reported, not pooled"); primary 1개; 금지 문구 0; T 동결 파일 sha16 불변(RR0 manifest 대조).

## 중단 조건
- S Q_SELECT에서 PT_flex 격자 선정이 실패(수렴 안 됨)하면 BLOCKED + 로그.
- 합집합 라벨에 키 중복이 있으면 BLOCKED.
- 계약 밖 arm이 필요해 보이면 실행하지 말고 "제안" 절에 기록.
